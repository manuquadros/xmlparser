# Changelog

Every released version of xmlparser, newest first, generated by
`pdm run changelog` from the commit history. Do not edit by hand.

## 0.2.0 — 2026-09-15

### Added

- Translate formatting for JATS files (`2a98576`)
- Add JatsFrontMeta and ParsedArticle (`42c96b7`)
- Parse_pubmed_article (`63f78dc`)

### Fixed

- Convert article_xml to string before adding it as exception note (`43701fb`)
- Fail on XMLSyntaxError (`5fa01bf`)
- Handle numeric prefixes on titles, removes redundant names and return utf-8 (`325b306`)
- Import-from importlib.resources (`4854631`)
- Lastname, firstname sequence separated by ; (`a7747fd`)
- Join all text nodes for article-title to handle formatting tags (`dc3a827`)
- Scope parse_jats_article XPaths to the given element (`eacd40e`)

### Changed

- Export XMLSyntaxError (`13bfbe5`)

### Internal

- Migrate to pdm (`af79c8c`)
- Add pre-commit hooks, lock, and requirements (`64fc6d6`)
- Add pre-commit hooks, lock, and requirements (`e86b9c7`)
- Add pytest-profiling and upgrade nltk (`e5fd939`)
- Adopt ncbitax's release infra (git-cliff, scm versioning) (`3028cf0`)
- Put src on pytest's pythonpath, ignore .pdm-python (`9f4d7bd`)

## 0.1.0 — 2025-10-06

### Added

- Transform_tree (`7b7e64a`)

### Fixed

- Return non-pretty-printed byte string from transform_article (`7fe70f6`)
- Py.typed (`b449a4a`)

### Changed

- Make stylesheets available through importlib.resources (`1924170`)

### Internal

- Add lxml dep (`58b0695`)
- Define exports (`fa65810`)
- Remove obsolete tests (`85eab54`)
- Formatting (`d44c424`)
- Add license (`9ee1cb7`)

